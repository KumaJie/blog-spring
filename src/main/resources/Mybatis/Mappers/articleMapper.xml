<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cqut.blogspring.dao.ArticleDao">
    <select id="selectByTitle" resultType="Article">
        select * from article where article_title = #{articleTitle}
    </select>

    <select id="selectListByUserId" resultType="Article">
        select * from article where user_id = #{userId} order by  article_date desc
    </select>

    <select id="selectListByViews" resultType="Article">
        select * from article order by article_view, article_date desc
    </select>

    <select id="selectList" resultType="Article">
        select * from article
    </select>

    <insert id="insert" parameterType="Article">
        insert into article(article_id, user_id, categories_id, article_title, article_content)
        value (#{articleId}, #{userId}, #{categoriesId}, #{articleTitle}, #{articleContent})
    </insert>

    <delete id="deleteByTitle" parameterType="string">
        delete from article where article_title = #{articleTitle}
    </delete>

    <update id="updateViewById" parameterType="string">
        update article
        set article_view = article_view + 1
        where article_id = #{articleId}
    </update>

    <update id="updateLikeById" parameterType="string">
        update article
        set article_like = article_like + 1
        where article_id = #{articleId}
    </update>
</mapper>